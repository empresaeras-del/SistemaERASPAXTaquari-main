# =============================================================================
# AMBIENTE DE HOMOLOGACAO — SistemaPaxTaquari-Homologacao
# =============================================================================
# Copie para `.env.local` (ou `.env`) quando quiser rodar o app contra homologacao
# em vez de producao:
#
#     cp .env.homologacao.example .env.local && npm run dev
#
# Este projeto NAO tem dado real. O banco e reconstruido por
# `supabase/migrations/` + `supabase/seed-homologacao.sql`, e pode ser apagado e
# recriado a qualquer momento.
#
# A chave abaixo e a ANON (publica), a mesma que ja vai no bundle do navegador em
# qualquer deploy — nao e segredo. A protecao e a RLS, nao a chave.
#
# Usuarios semeados (senha: homologacao123):
#   super@homologacao.local        super_admin   ve as duas empresas
#   admin.pax@homologacao.local    admin         PAX Homologacao
#   gerente.pax@homologacao.local  gerente       PAX, so modulos associados+atendimentos
#   func.pax@homologacao.local     funcionario   PAX, so modulos associados+financeiro
#   admin.fun@homologacao.local    admin         Funeraria Teste
#
# O gerente e o funcionario existem para exercitar a RLS por modulo
# (migration 20260919001244): os dois LEEM planos_pax e sao RECUSADOS ao gravar.
# =============================================================================

VITE_SUPABASE_URL="https://rwbqkehwwdbdhcgcfcgf.supabase.co"
VITE_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJ3YnFrZWh3d2RiZGhjZ2NmY2dmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODk3NzM4NjQsImV4cCI6MjEwNTM0OTg2NH0.xxAix82G3svsV-O4kOquWnf22Vo9GAlQuvZIZhtWHRk"

VITE_GEMINI_API_KEY=""
VITE_SENTRY_DSN=""
APP_URL="http://localhost:5173"
